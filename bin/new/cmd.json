{
  "verb": "new",
  "summary": "scaffold a project (wiki hub + ~/work repo), client (wiki hub + ~/files tree), or verb (bin/<name>/ stub)",
  "usage": "plainkeep new project \"<name>\" [--kind labs|products|tools] | new client \"<name>\" | new verb <name> [--risk <class>] [--summary \"<text>\"]",
  "risk": "safe_write",
  "args": [
    {
      "name": "type",
      "required": true
    },
    {
      "name": "name",
      "required": true
    }
  ],
  "reads": [
    "templates/project-repo/",
    "templates/verb/",
    "wiki/"
  ],
  "writes": [
    "wiki/",
    "~/work/",
    "~/files/clients/",
    "bin/ (new verb)",
    "plainkeep.json",
    "journal/"
  ],
  "hints": "Scaffold a project (wiki hub + ~/work repo), client (hub + ~/files tree), or verb. Common mistake: a slug that collides \u2014 slugs are globally unique across the three roots.",
  "output": {
    "mode": "scalar",
    "fields": {
      "type": "string",
      "slug": "string",
      "hub": "string"
    }
  },
  "dry_run": true,
  "actions": [
    {
      "name": "project",
      "summary": "scaffold a project: wiki hub + ~/work repo",
      "risk": "safe_write",
      "dry_run": true,
      "args": [
        { "name": "name", "type": "string", "required": true, "help": "the project name (slugified)", "example": "Acme Redesign" },
        { "name": "--kind", "type": "enum", "required": false, "default": "labs", "enum": ["products", "labs", "tools"], "help": "the ~/work subtree for the repo" }
      ]
    },
    {
      "name": "client",
      "summary": "scaffold a client: wiki hub + ~/files tree",
      "risk": "safe_write",
      "dry_run": true,
      "args": [
        { "name": "name", "type": "string", "required": true, "help": "the client name (slugified)", "example": "Acme Inc" }
      ]
    },
    {
      "name": "verb",
      "summary": "scaffold a new bin/<name>/ command stub",
      "risk": "safe_write",
      "dry_run": true,
      "args": [
        { "name": "name", "type": "slug", "required": true, "help": "the verb name (the bin/<name>/ dir)", "example": "digest" },
        { "name": "--risk", "type": "enum", "required": false, "enum": ["read", "safe_write", "draft_only", "confirm", "deny"], "help": "the guardrail class for the new verb" },
        { "name": "--summary", "type": "string", "required": false, "help": "the one-line summary for the stub's cmd.json" }
      ]
    }
  ]
}
